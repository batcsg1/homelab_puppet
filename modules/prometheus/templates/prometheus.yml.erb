global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - "/usr/local/bin/prometheus/record_rules.yml"
  - "/usr/local/bin/prometheus/alert_rules.yml"

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets:
        - "localhost:9090"

  - job_name: "node-exporter"
    file_sd_configs:
      - files:
        - "/etc/prometheus/targets/*.yml"
        refresh_interval: 30s
